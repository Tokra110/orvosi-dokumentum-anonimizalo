-r requirements.txt

# Export-only dependencies. These are intentionally excluded from runtime
# requirements because they pull in the large PyTorch stack.
docling>=2.96,<2.97
torch
onnx>=1.16
onnxscript>=0.5
# runtime dropped transformers (tokenizers-only NER); export still needs it
transformers>=4.40,<5
